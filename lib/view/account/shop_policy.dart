import 'package:flutter/material.dart';

class ShopPolicy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Chính sách bán hàng'),
      ),
      body: Container(),
    );
  }
}
